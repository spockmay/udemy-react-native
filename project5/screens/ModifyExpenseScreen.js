import { View, Text } from 'react-native'

function ModifyExpenseScreen({navigation, route}) {
    return (
        <View>
            <Text>Modify an Expense</Text>
        </View>
    );
}

export default ModifyExpenseScreen;